{
  "name": "rag-using-langchain",
  "private": true,
  "scripts": {
    "dev": "python3 -m http.server 8080 --directory site",
    "build": "echo \"no build step\"",
    "build:dev": "echo \"no build step\""
  }
}
